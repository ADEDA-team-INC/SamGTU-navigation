import React from 'react'
import { Back } from '../UI/Back'

export const SightsPage = ({ }) => {
    return (
        <div>
            <Back />
            <p style={{color: '#000'}}>
                Ничего не найдено
            </p>
        </div>
    )
}