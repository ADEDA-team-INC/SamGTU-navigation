import React from 'react'
import { NavLink } from 'react-router-dom'

export const BuildingsPage = ({ }) => {
    return (
        <div style={{ color: '#000' }}>
            <NavLink to={'/'}>
                Домой
            </NavLink>
            color
        </div>
    )
}